package com.chaddrennan;

public class Main {

    public static void main(String[] args) {
	// Reverse a sequence of strings

        // create an empty stack s
        // s is a reference that refers to a sos object
        // calling a generic type object requires a type
        /*
        StackOfStrings<Song> s = new StackOfStrings<Song>();

        // push some strings on to the stack
        s.push(new Song("Friday", "Rebecca B."));
        s.push(new Song("Summertime", "Whoever"));
        //s.push("friend");

        //pop all items and print them out
        while (!s.isEmpty()) {
            System.out.println(s.pop());
        }
        System.out.println("Done!");
         */
    }
}
